{
  "name": "info-sec-memo",
  "version": "1.0.0",
  "description": "정보보안기사 기출 요약집 — 손글씨 원본 암기 노트",
  "private": true,
  "scripts": {
    "start": "serve -s . -l tcp://0.0.0.0:${PORT:-3000}",
    "dev": "serve . -l 8080"
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "engines": {
    "node": ">=18"
  }
}
